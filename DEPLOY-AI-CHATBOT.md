# 🚀 Швидкий Деплой AI Чат-бота

## Крок 1: Отримати Gemini API Key

1. Перейти на https://ai.google.dev/
2. Натиснути "Get API key"
3. Вибрати або створити проект
4. Згенерувати API key
5. Скопіювати ключ

## Крок 2: Встановити Supabase CLI

```bash
npm install -g supabase
```

## Крок 3: Залогінитись в Supabase

```bash
supabase login
```

Відкриється браузер для авторизації.

## Крок 4: Прилінкувати проект

```bash
cd "c:\Users\andri\OneDrive\Projects\агенство-новий"
supabase link --project-ref cinufkskitdiuonfibtt
```

Введи пароль від бази даних якщо попросить.

## Крок 5: Встановити Gemini API Key

```bash
supabase secrets set GEMINI_API_KEY=ваш_ключ_тут
```

Замінити `ваш_ключ_тут` на реальний ключ з Крок 1.

## Крок 6: Задеплоїти функцію

```bash
supabase functions deploy ai-chat
```

Почекати ~30 секунд. Побачиш:
```
Deployed Function ai-chat version: ...
Function URL: https://cinufkskitdiuonfibtt.supabase.co/functions/v1/ai-chat
```

## Крок 7: Перевірити роботу

Відкрити будь-яку сторінку сайту (ua/index.html, de/index.html), натиснути на чат-бот і написати питання:

- "Скільки коштує Google Ads?"
- "Wie viel kostet Google Ads?"
- "How much does Google Ads cost?"
- "Ile kosztuje Google Ads?"

Чат-бот має відповісти розумно на твоїй мові!

## Діагностика проблем

### Помилка "GEMINI_API_KEY not configured"

```bash
supabase secrets list
# Перевір чи є GEMINI_API_KEY
# Якщо немає - виконай Крок 5 ще раз
```

### Помилка "Failed to fetch"

Перевір CORS. Функція вже налаштована на `'Access-Control-Allow-Origin': '*'`.

### Переглянути логи функції

```bash
supabase functions logs ai-chat --limit 50
```

### Локальне тестування

```bash
# Запустити локально
supabase functions serve ai-chat

# В іншому терміналі протестувати
curl -X POST http://localhost:54321/functions/v1/ai-chat \
  -H "Content-Type: application/json" \
  -d "{\"message\": \"Скільки коштує?\", \"language\": \"ua\"}"
```

## Готово! 🎉

Тепер чат-бот використовує Gemini AI замість простих keywords.
Він розуміє контекст, відповідає природньою мовою і може вести справжню розмову.
